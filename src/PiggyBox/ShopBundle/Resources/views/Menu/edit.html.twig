<h1>Menu edit</h1>

<form action="{{ path('formule_update', { 'id': menu.id }) }}" method="post" {{ form_enctype(edit_form) }}>

	{% for  menuItem in edit_form.menuItems %}
		{{ form_widget(menuItem.title) }}
		{{ form_widget(menuItem.products) }}
	{% endfor %}
    {{ form_widget(edit_form) }}

    <p>
        <button type="submit">Edit</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('formule') }}">
            Back to the list
        </a>
    </li>
    <li>
        <form action="{{ path('formule_delete', { 'id': menu.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
