<h1>Product</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ entity.name }}</td>
        </tr>
        <tr>
            <th>Description</th>
            <td>{{ entity.description }}</td>
        </tr>
        <tr>
            <th>Price_kg</th>
            <td>{{ entity.pricekg }}</td>
        </tr>
        <tr>
            <th>Active</th>
            <td>{{ entity.active }}</td>
        </tr>
        <tr>
            <th>Image_path</th>
            <td>{{ entity.imagepath }}</td>
        </tr>
        <tr>
            <th>Promo_active</th>
            <td>{{ entity.promoactive }}</td>
        </tr>
        <tr>
            <th>Promo_price</th>
            <td>{{ entity.promoprice }}</td>
        </tr>
        <tr>
            <th>Promo_expire_date</th>
            <td>{{ entity.promoexpiredate|date('Y-m-d H:i:s') }}</td>
        </tr>
        <tr>
            <th>Promo_percentage</th>
            <td>{{ entity.promopercentage }}</td>
        </tr>
        <tr>
            <th>Createdat</th>
            <td>{{ entity.createdat|date('Y-m-d H:i:s') }}</td>
        </tr>
        <tr>
            <th>Updatedat</th>
            <td>{{ entity.updatedat|date('Y-m-d H:i:s') }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('monmagasin_mesproduits') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('monmagasin_mesproduits_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('monmagasin_mesproduits_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
